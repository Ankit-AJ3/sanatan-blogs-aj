# Public URL of the site (used for SEO: canonical URLs, sitemap, OG tags)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Better Auth
BETTER_AUTH_URL=http://localhost:3000
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
BETTER_AUTH_SECRET=replace-with-a-long-random-string

# Google OAuth — https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database — local SQLite file for dev; use a Turso (libsql://...) URL + token in production
DATABASE_URL=file:./data/sanatan.db
DATABASE_AUTH_TOKEN=

# Comma-separated Google emails that can write/edit/delete articles
ADMIN_EMAILS=you@gmail.com
